--- eject-2.0.13/eject.c.orig	2002-12-09 23:28:08.000000000 +0100
+++ eject-2.0.13/eject.c	2004-10-31 04:54:16.858729688 +0100
@@ -321,6 +321,11 @@
 	if (FileExists(buf))
 		return buf;
 
+	strcpy(buf, "/media/");
+	strcat(buf, name);
+	if (FileExists(buf))
+		return buf;
+
 	strcpy(buf, "/mnt/");
 	strcat(buf, name);
 	if (FileExists(buf))
